apiVersion: v1
kind: Service
metadata:
  name: hello-kustomize
  labels:
    app.kubernetes.io/name: hello-kustomize
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app.kubernetes.io/name: hello-kustomize

# Shell-sourced by plugins/custom-render-plugin/generate.sh — both vars are
# optional there (it falls back to sensible defaults if this file is absent).
MESSAGE="Hello World from hello-plugin"
REPLICAS=1

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: hello-timoni-values
  namespace: argocd
spec:
  project: hello-world-plugins-lab
  source:
    repoURL: "https://github.com/xss603/hello-world-plugins-lab.git"
    targetRevision: main
    path: apps/hello-timoni-values
    plugin:
      name: timoni-plugin
  destination:
    server: https://kubernetes.default.svc
    namespace: hello-world-plugins-lab
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
